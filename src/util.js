const isEven = (number) => number % 2 === 0;

const getRandomInt = (min, max) => Math.floor(Math.random() * (max - min)) + min;

const getExpression = () => true;

export { isEven, getRandomInt, getExpression };
